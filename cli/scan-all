#!/bin/bash
# scan-all - Run all scanners across today + previous days.

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$ROOT_DIR/cron/realtime-scan" "$@"
"$ROOT_DIR/cron/daily-scan" "$@"
exec "$ROOT_DIR/cron/weekly-scan" "$@"
