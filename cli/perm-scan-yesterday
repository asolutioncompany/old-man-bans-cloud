#!/bin/bash
# perm-scan-yesterday - Run daily scanner once.

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck disable=SC1091
source "$ROOT_DIR/lib/common-lib"
run_scanner_script "$ROOT_DIR" "cron/cron-daily-scan" "$@"
